import { Config } from '../common/type';

const config: Config = {
    name: 'Floor',
    width: '100%',
    x: 0,
    y: 0,
    height: 650,
    canDrag: false,
    canAddAsChild: true,
};

export default config;
